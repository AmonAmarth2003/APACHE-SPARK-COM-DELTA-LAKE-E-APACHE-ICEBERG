[project]
name = "spark-iceberg"
version = "0.1.0"
description = "Projeto Apache Spark com Apache Iceberg"
requires-python = ">=3.11"
dependencies = [
    "pyspark (==3.5.3)",
    "ipykernel (==6.29.5)",
]
